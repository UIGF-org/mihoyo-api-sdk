// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::game::genshin::dispatch::current_region::generated::clients::CurrentRegionCurrentRegionApiClient;
use azure_core::{
    Result,
    fmt::SafeDebug,
    http::{ClientOptions, Pipeline, Url},
    tracing,
};

/// Resolves the current Genshin region from the CN game dispatch service.
#[tracing::client]
pub struct CurrentRegionClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`CurrentRegionClient`](CurrentRegionClient)
#[derive(Clone, Default, SafeDebug)]
pub struct CurrentRegionClientOptions {
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl CurrentRegionClient {
    /// Creates a new CurrentRegionClient requiring no authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `options` - Optional configuration for the client.
    #[tracing::new("UIGF.Mihoyo.Game.Genshin.Dispatch.CurrentRegion")]
    pub fn with_no_credential(
        endpoint: &str,
        options: Option<CurrentRegionClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                format!("{endpoint} must use http(s)"),
            ));
        }
        Ok(Self {
            endpoint,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                Vec::default(),
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Returns a new instance of CurrentRegionCurrentRegionApiClient.
    #[tracing::subclient]
    pub fn get_current_region_current_region_api_client(
        &self,
    ) -> CurrentRegionCurrentRegionApiClient {
        CurrentRegionCurrentRegionApiClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }
}
