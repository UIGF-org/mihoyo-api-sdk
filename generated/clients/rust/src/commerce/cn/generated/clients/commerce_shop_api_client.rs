// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    commerce::{
        cn::generated::models::{
            CommerceShopApiClientCheckOrderOptions, CommerceShopApiClientCreateOrderOptions,
            CommerceShopApiClientFetchGoodsOptions,
            CommerceShopApiClientGetCurrencyAndCountryByIPOptions,
        },
        models::{CreateOrderRequest, ShopGoodsRequest},
    },
    models::{
        ApiResponseCurrencyAndCountry, ApiResponseJsonObject, ApiResponseOrderStatus,
        ApiResponseShopGoods,
    },
};
use azure_core::{
    Result,
    error::CheckSuccessOptions,
    http::{Method, Pipeline, PipelineSendOptions, Request, RequestContent, Response, Url, UrlExt},
    tracing,
};

#[tracing::client]
pub struct CommerceShopApiClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl CommerceShopApiClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Commerce.CN.ShopApi.checkOrder")]
    pub async fn check_order(
        &self,
        game_biz: &str,
        order_no: &str,
        game: &str,
        region: &str,
        uid: &str,
        options: Option<CommerceShopApiClientCheckOrderOptions<'_>>,
    ) -> Result<Response<ApiResponseOrderStatus>> {
        if game_biz.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game_biz cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/{gameBiz}/mdk/atropos/api/checkOrder");
        path = path.replace("{gameBiz}", game_biz);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("game", game);
        query_builder.set_pair("order_no", order_no);
        query_builder.set_pair("region", region);
        query_builder.set_pair("uid", uid);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        if let Some(cookie) = options.cookie.as_ref() {
            request.insert_header("cookie", cookie);
        }
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Sends a vendor-signed order request; no payment signing is generated by this SDK.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Commerce.CN.ShopApi.createOrder")]
    pub async fn create_order(
        &self,
        game_biz: &str,
        cookie: String,
        device_id: String,
        body: RequestContent<CreateOrderRequest>,
        options: Option<CommerceShopApiClientCreateOrderOptions<'_>>,
    ) -> Result<Response<ApiResponseJsonObject>> {
        if game_biz.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game_biz cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/{gameBiz}/mdk/atropos/api/createOrder");
        path = path.replace("{gameBiz}", game_biz);
        url.append_path(&path);
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.insert_header("cookie", cookie);
        request.insert_header("x-rpc-device_id", device_id);
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Returns purchasable catalogue entries for the authenticated game account.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Commerce.CN.ShopApi.fetchGoods")]
    pub async fn fetch_goods(
        &self,
        game_biz: &str,
        body: RequestContent<ShopGoodsRequest>,
        options: Option<CommerceShopApiClientFetchGoodsOptions<'_>>,
    ) -> Result<Response<ApiResponseShopGoods>> {
        if game_biz.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game_biz cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/{gameBiz}/mdk/shopwindow/shopwindow/fetchGoods");
        path = path.replace("{gameBiz}", game_biz);
        url.append_path(&path);
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        if let Some(cookie) = options.cookie.as_ref() {
            request.insert_header("cookie", cookie);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Returns the storefront currency and country.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Commerce.CN.ShopApi.getCurrencyAndCountryByIp")]
    pub async fn get_currency_and_country_by_ip(
        &self,
        game_biz: &str,
        options: Option<CommerceShopApiClientGetCurrencyAndCountryByIPOptions<'_>>,
    ) -> Result<Response<ApiResponseCurrencyAndCountry>> {
        if game_biz.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game_biz cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path =
            String::from("/{gameBiz}/mdk/shopwindow/shopwindow/getCurrencyAndCountryByIp");
        path = path.replace("{gameBiz}", game_biz);
        url.append_path(&path);
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        if let Some(body) = options.body.clone() {
            request.insert_header("content-type", "application/json");
            request.set_body(body);
        }
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
