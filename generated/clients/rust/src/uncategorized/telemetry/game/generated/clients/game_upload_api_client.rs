// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    api::request::models::{
        GameCrashTelemetryEvent, GameCustomTelemetryEvent, GameSdkTelemetryEvent,
    },
    uncategorized::{
        models::UploadResponse,
        telemetry::game::generated::models::{
            GameUploadApiClientUploadCrashOptions, GameUploadApiClientUploadCustomOptions,
            GameUploadApiClientUploadSdkOptions,
        },
    },
};
use azure_core::{
    Result,
    error::CheckSuccessOptions,
    http::{Method, Pipeline, PipelineSendOptions, Request, RequestContent, Response, Url, UrlExt},
    tracing,
};

#[tracing::client]
pub struct GameUploadApiClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl GameUploadApiClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// The official client signs the upload headers; signing is intentionally caller-owned.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Uncategorized.Telemetry.Game.UploadApi.uploadCrash")]
    pub async fn upload_crash(
        &self,
        body: RequestContent<Vec<GameCrashTelemetryEvent>>,
        options: Option<GameUploadApiClientUploadCrashOptions<'_>>,
    ) -> Result<Response<UploadResponse>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/crash/dataUpload");
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        if let Some(authorization) = options.authorization.as_ref() {
            request.insert_header("authorization", authorization);
        }
        if let Some(content_md5) = options.content_md5.as_ref() {
            request.insert_header("content-md5", content_md5);
        }
        request.insert_header("content-type", "application/json");
        if let Some(date) = options.date.as_ref() {
            request.insert_header("date", date);
        }
        if let Some(cms_signature) = options.cms_signature.as_ref() {
            request.insert_header("cms-signature", cms_signature);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Uncategorized.Telemetry.Game.UploadApi.uploadCustom")]
    pub async fn upload_custom(
        &self,
        body: RequestContent<Vec<GameCustomTelemetryEvent>>,
        options: Option<GameUploadApiClientUploadCustomOptions<'_>>,
    ) -> Result<Response<UploadResponse>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/ys_custom/dataUpload");
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        if let Some(authorization) = options.authorization.as_ref() {
            request.insert_header("authorization", authorization);
        }
        if let Some(content_md5) = options.content_md5.as_ref() {
            request.insert_header("content-md5", content_md5);
        }
        request.insert_header("content-type", "application/json");
        if let Some(date) = options.date.as_ref() {
            request.insert_header("date", date);
        }
        if let Some(cms_signature) = options.cms_signature.as_ref() {
            request.insert_header("cms-signature", cms_signature);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Uncategorized.Telemetry.Game.UploadApi.uploadSdk")]
    pub async fn upload_sdk(
        &self,
        body: RequestContent<Vec<GameSdkTelemetryEvent>>,
        options: Option<GameUploadApiClientUploadSdkOptions<'_>>,
    ) -> Result<Response<UploadResponse>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/sdk/dataUpload");
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        if let Some(authorization) = options.authorization.as_ref() {
            request.insert_header("authorization", authorization);
        }
        if let Some(content_md5) = options.content_md5.as_ref() {
            request.insert_header("content-md5", content_md5);
        }
        request.insert_header("content-type", "application/json");
        if let Some(date) = options.date.as_ref() {
            request.insert_header("date", date);
        }
        if let Some(cms_signature) = options.cms_signature.as_ref() {
            request.insert_header("cms-signature", cms_signature);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
