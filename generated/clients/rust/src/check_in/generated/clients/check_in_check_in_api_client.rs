// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    check_in::generated::models::{
        CheckInCheckInApiClientGetHomeOptions, CheckInCheckInApiClientGetInfoOptions,
        CheckInCheckInApiClientGetResignInfoOptions, CheckInCheckInApiClientResignOptions,
        CheckInCheckInApiClientSignOptions,
    },
    community::models::LunaCheckInRequest,
    models::ApiResponseJsonObject,
};
use azure_core::{
    Result,
    error::CheckSuccessOptions,
    http::{Method, Pipeline, PipelineSendOptions, Request, RequestContent, Response, Url, UrlExt},
    tracing,
};

#[tracing::client]
pub struct CheckInCheckInApiClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl CheckInCheckInApiClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.CheckIn.CheckInApi.getHome")]
    pub async fn get_home(
        &self,
        game: &str,
        cookie: String,
        lang: &str,
        act_id: &str,
        options: Option<CheckInCheckInApiClientGetHomeOptions<'_>>,
    ) -> Result<Response<ApiResponseJsonObject>> {
        if game.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/event/luna/{game}/home");
        path = path.replace("{game}", game);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("act_id", act_id);
        query_builder.set_pair("lang", lang);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        request.insert_header("cookie", cookie);
        if let Some(sign_game) = options.sign_game.as_ref() {
            request.insert_header("x-rpc-signgame", sign_game);
        }
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.CheckIn.CheckInApi.getInfo")]
    pub async fn get_info(
        &self,
        game: &str,
        cookie: String,
        lang: &str,
        act_id: &str,
        options: Option<CheckInCheckInApiClientGetInfoOptions<'_>>,
    ) -> Result<Response<ApiResponseJsonObject>> {
        if game.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/event/luna/{game}/info");
        path = path.replace("{game}", game);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("act_id", act_id);
        query_builder.set_pair("lang", lang);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        request.insert_header("cookie", cookie);
        if let Some(sign_game) = options.sign_game.as_ref() {
            request.insert_header("x-rpc-signgame", sign_game);
        }
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.CheckIn.CheckInApi.getResignInfo")]
    pub async fn get_resign_info(
        &self,
        game: &str,
        cookie: String,
        lang: &str,
        act_id: &str,
        options: Option<CheckInCheckInApiClientGetResignInfoOptions<'_>>,
    ) -> Result<Response<ApiResponseJsonObject>> {
        if game.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/event/luna/{game}/resign_info");
        path = path.replace("{game}", game);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("act_id", act_id);
        query_builder.set_pair("lang", lang);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        request.insert_header("cookie", cookie);
        if let Some(sign_game) = options.sign_game.as_ref() {
            request.insert_header("x-rpc-signgame", sign_game);
        }
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.CheckIn.CheckInApi.resign")]
    pub async fn resign(
        &self,
        game: &str,
        cookie: String,
        body: RequestContent<LunaCheckInRequest>,
        options: Option<CheckInCheckInApiClientResignOptions<'_>>,
    ) -> Result<Response<ApiResponseJsonObject>> {
        if game.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/event/luna/{game}/resign");
        path = path.replace("{game}", game);
        url.append_path(&path);
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.insert_header("cookie", cookie);
        if let Some(sign_game) = options.sign_game.as_ref() {
            request.insert_header("x-rpc-signgame", sign_game);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.CheckIn.CheckInApi.sign")]
    pub async fn sign(
        &self,
        game: &str,
        cookie: String,
        body: RequestContent<LunaCheckInRequest>,
        options: Option<CheckInCheckInApiClientSignOptions<'_>>,
    ) -> Result<Response<ApiResponseJsonObject>> {
        if game.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter game cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/event/luna/{game}/sign");
        path = path.replace("{game}", game);
        url.append_path(&path);
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.insert_header("cookie", cookie);
        if let Some(sign_game) = options.sign_game.as_ref() {
            request.insert_header("x-rpc-signgame", sign_game);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
