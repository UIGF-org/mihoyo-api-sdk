// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    launcher::{
        global::generated::models::{
            GlobalLauncherMetadataApiClientGetAllGameBasicInfoOptions,
            GlobalLauncherMetadataApiClientGetDeprecatedFileConfigsOptions,
            GlobalLauncherMetadataApiClientGetDxConfigsOptions,
            GlobalLauncherMetadataApiClientGetGameBranchesOptions,
            GlobalLauncherMetadataApiClientGetGameChannelSdksOptions,
            GlobalLauncherMetadataApiClientGetGameConfigsOptions,
            GlobalLauncherMetadataApiClientGetGamePackagesOptions,
            GlobalLauncherMetadataApiClientGetGameScanInfoOptions,
            GlobalLauncherMetadataApiClientGetWpfPackagesOptions,
        },
        models::DxConfigRequest,
    },
    models::{LauncherResponseGameBasicInfo, LauncherResponseJsonObject},
};
use azure_core::{
    Result,
    error::CheckSuccessOptions,
    http::{Method, Pipeline, PipelineSendOptions, Request, RequestContent, Response, Url, UrlExt},
    tracing,
};

#[tracing::client]
pub struct GlobalLauncherMetadataApiClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl GlobalLauncherMetadataApiClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getAllGameBasicInfo")]
    pub async fn get_all_game_basic_info(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetAllGameBasicInfoOptions<'_>>,
    ) -> Result<Response<LauncherResponseGameBasicInfo>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getAllGameBasicInfo");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getDeprecatedFileConfigs")]
    pub async fn get_deprecated_file_configs(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetDeprecatedFileConfigsOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getGameDeprecatedFileConfigs");
        let mut query_builder = url.query_builder();
        if let Some(channel) = options.channel {
            query_builder.set_pair("channel", channel.to_string());
        }
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        if let Some(sub_channel) = options.sub_channel {
            query_builder.set_pair("sub_channel", sub_channel.to_string());
        }
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getDxConfigs")]
    pub async fn get_dx_configs(
        &self,
        launcher_id: &str,
        language: &str,
        body: RequestContent<DxConfigRequest>,
        options: Option<GlobalLauncherMetadataApiClientGetDxConfigsOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getDXConfigs");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getGameBranches")]
    pub async fn get_game_branches(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetGameBranchesOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getGameBranches");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getGameChannelSdks")]
    pub async fn get_game_channel_sdks(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetGameChannelSdksOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getGameChannelSDKs");
        let mut query_builder = url.query_builder();
        if let Some(channel) = options.channel {
            query_builder.set_pair("channel", channel.to_string());
        }
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        if let Some(sub_channel) = options.sub_channel {
            query_builder.set_pair("sub_channel", sub_channel.to_string());
        }
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getGameConfigs")]
    pub async fn get_game_configs(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetGameConfigsOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getGameConfigs");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getGamePackages")]
    pub async fn get_game_packages(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetGamePackagesOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getGamePackages");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getGameScanInfo")]
    pub async fn get_game_scan_info(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetGameScanInfoOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getGameScanInfo");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("UIGF.Mihoyo.Launcher.Global.MetadataApi.getWpfPackages")]
    pub async fn get_wpf_packages(
        &self,
        launcher_id: &str,
        language: &str,
        options: Option<GlobalLauncherMetadataApiClientGetWpfPackagesOptions<'_>>,
    ) -> Result<Response<LauncherResponseJsonObject>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/hyp/hyp-connect/api/getWPFPackages");
        let mut query_builder = url.query_builder();
        if let Some(game_ids) = options.game_ids.as_ref() {
            query_builder.set_pair("game_ids[]", game_ids.join(","));
        }
        query_builder.set_pair("language", language);
        query_builder.set_pair("launcher_id", launcher_id);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
